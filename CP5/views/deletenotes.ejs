<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Note</title>
    <style>
        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 2rem;
            background: linear-gradient(to bottom, #FF4B2B, #FFFFFF); /* Red to White Gradient */
            color: #f5f3e7;
        }

        button, input[type="submit"] {
            background: linear-gradient(to bottom, #e51c23, #FF4B2B); /* Gradient red */
            border: 2px solid #d32f2f;
            padding: 10px 20px;
            font-weight: bold;
            font-family: 'Georgia', serif;
            cursor: pointer;
            box-shadow: 2px 2px 4px rgba(0,0,0,0.4);
            border-radius: 6px;
            color: #fdfaf5;
            margin: 0.5rem;
        }

        a {
            text-decoration: none;
            color: #d32f2f;
            font-weight: bold;
        }

        h1, h2, h3 {
            font-family: 'Georgia', serif;
            border-bottom: 2px solid #c2185b;
            padding-bottom: 5px;
            background: linear-gradient(to right, #e91e63, #d32f2f); /* Red gradient */
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            margin: 1rem 0;
            color: #f5e9d6;
        }

        label {
            font-weight: bold;
            font-size: 1.1rem;
            color: #e91e63;
        }

        input[type="text"] {
            width: 100%;
            max-width: 600px;
            padding: 0.5rem;
            margin-top: 0.3rem;
            margin-bottom: 1rem;
            border: 1px solid #d32f2f;
            background: linear-gradient(to bottom right, #FFCDD2, #f44336); /* Soft red to white */
            border-radius: 5px;
            font-family: 'Georgia', serif;
            color: #f5f3e7;
        }

        form {
            background: linear-gradient(to bottom right, #FF5252, #FFEBEE); /* Light red to white */
            border: 2px solid #e57373;
            padding: 2rem;
            border-radius: 15px;
            max-width: 700px;
            margin: 2rem auto;
            box-shadow: 5px 5px 15px rgba(0,0,0,0.3);
        }

        /* Separate each note with boxes */
        .note-box {
            background: linear-gradient(to top left, #f44336, #FFCDD2); /* Soft red tones */
            border: 2px solid #d32f2f;
            padding: 1.5rem;
            margin-top: 1.5rem;
            border-radius: 15px;
            box-shadow: 4px 4px 12px rgba(0,0,0,0.3);
        }

        .note-box h1 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .note-box h3 {
            font-size: 1.2rem;
            margin-top: 0.5rem;
        }

        .note-box a {
            display: inline-block;
            color: #0f0f0e;
            font-size: 1.1rem;
            text-decoration: underline;
            margin-top: 0.5rem;
        }

        .note-box p {
            background: #d32f2f;
            padding: 1rem;
            border-left: 4px solid #c2185b;
            margin-top: 0.5rem;
            border-radius: 4px;
            color: #f4efe7;
        }
    </style>
</head>
<body>
    <form method="get" action="/delNote">
        <label for="bt">Enter Book Title to Delete:</label>
        <input type="text" name="bt" id="bt"/>
        <input type="submit" value="Delete"/>
    </form>

    <h1>The Notes You Have Written</h1>

    <% bookslist.forEach((element) => { %>
        <div class="note-box">
            <h1><%= element.bookTitle %></h1>
            <h3><%= element.brief %></h3>
            <a href="<%= element.linkofbook %>" target="_blank">Link to book</a>
            <p><%= element.notes %></p>
        </div>
    <% }); %>
</body>
</html>
