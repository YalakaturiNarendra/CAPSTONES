<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
    body {
        font-family: 'Georgia', serif;
        background: linear-gradient(to bottom, #FF4B2B, #FFFFFF); /* Red to White Gradient */
        margin: 0;
        padding: 2rem;
        color: #f5f3e7;
    }

    button, input[type="submit"] {
        background: linear-gradient(to bottom, #e51c23, #FF4B2B); /* Gradient red */
        border: 2px dashed #d32f2f;
        padding: 10px 20px;
        font-weight: bold;
        font-family: 'Georgia', serif;
        cursor: pointer;
        box-shadow: 2px 2px 4px rgba(0,0,0,0.4);
        border-radius: 6px;
        color: #fdfaf5;
        margin: 0.5rem;
    }

    a {
        text-decoration: none;
        color: #d32f2f;
    }

    h1, h2, h3 {
        font-family: 'Georgia', serif;
        border-bottom: 2px dashed #c2185b;
        padding-bottom: 5px;
        background: linear-gradient(to right, #e91e63, #d32f2f); /* Red gradient */
        display: inline-block;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        margin: 1rem 0;
        color: #f5e9d6;
    }

    label {
        font-weight: bold;
        font-size: 1.1rem;
        color: #e4d3b2;
    }

    input[type="text"],
    input[type="url"],
    textarea {
        width: 100%;
        max-width: 600px;
        padding: 0.5rem;
        margin-top: 0.3rem;
        margin-bottom: 1rem;
        border: 1px solid #d32f2f;
        background: linear-gradient(to bottom right, #fce4ec, #f8bbd0); /* Light pink gradient */
        border-radius: 5px;
        font-family: 'Georgia', serif;
        color: #f5f3e7;
    }

    /* Updated the background for the "update input text box" */
    #update-form input[type="text"] {
        background: linear-gradient(to bottom right, #FFCDD2, #f44336); /* Red gradient */
    }

    form {
        background: linear-gradient(to bottom right, #fce4ec, #f8bbd0); /* Light pink gradient */
        border: 2px dashed #d32f2f;
        padding: 2rem;
        border-radius: 15px;
        max-width: 700px;
        margin: 2rem auto;
        box-shadow: 5px 5px 15px rgba(0,0,0,0.3);
    }

    /* Separate each note with color-toned boxes */
    .note-box {
        background: linear-gradient(to top left, #FFCDD2, #f44336); /* Soft pink to deep red gradient */
        border: 2px dashed #d32f2f;
        padding: 1.5rem;
        margin-top: 1.5rem;
        border-radius: 15px;
        box-shadow: 4px 4px 12px rgba(0,0,0,0.3);
    }

    .note-box h1 {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
    }

    .note-box h3 {
        font-size: 1.2rem;
        margin-top: 0.5rem;
    }

    .note-box a {
        display: inline-block;
        color: #d32f2f;
        font-size: 1.1rem;
        text-decoration: underline;
        margin-top: 0.5rem;
    }

    .note-box p {
        background: #d32f2f;
        padding: 1rem;
        border-left: 4px solid #c2185b;
        margin-top: 0.5rem;
        border-radius: 4px;
        color: #f4efe7;
    }

    #update-form {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: linear-gradient(to top left, #FFCDD2, #f44336); /* Matching note box gradient */
    border: 2px dashed #b28b58;
    padding: 1.5rem 2rem;
    border-radius: 15px;
    box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    font-family: 'Georgia', serif;
    max-width: 800px;
    margin: 0 auto 2rem auto;
    width: 90%;
}


    #update-form label {
        font-weight: bold;
        font-size: 1rem;
        color: #4a3c2f;
        display: block;
        margin-top: 1rem;
    }

    #update-form input[type="text"],
    #update-form input[type="url"],
    #update-form textarea {
        width: 100%;
        padding: 0.5rem;
        margin-top: 0.3rem;
        border: 1px solid #bfa97f;
        background: linear-gradient(to bottom right, #fffefb, #f8f3e3);
        border-radius: 5px;
        font-family: 'Georgia', serif;
        color: black;
    }

    /* Style the update button box with similar styles as the note boxes */
    #update-form input[type="submit"] {
        background: linear-gradient(to top left, #FFCDD2, #f44336); /* Red gradient */
        border: 2px dashed #d32f2f;
        padding: 12px 20px;
        font-weight: bold;
        cursor: pointer;
        border-radius: 15px;
        box-shadow: 2px 2px 4px rgba(0,0,0,0.4);
        color: #fdfaf5;
        margin-top: 1.5rem;
    }

    #update-form input[type="submit"]:hover {
        background: linear-gradient(to bottom, #e51c23, #FF4B2B); /* Gradient red */
    }

    </style>
</head>
<body>
    <form id="update-form" method="get" action="/update-note">
        <input type="text" name="bookt" required/>
        <input type="submit" value="Update"/>
    </form>

    <h1>The Notes You Have Written</h1>

    <% bookslist.forEach((element) => { %>
        <div class="note-box">
            <h1><%= element.bookTitle %></h1>
            <h3><%= element.brief %></h3>
            <a href="<%= element.linkofbook %>" target="_blank">Link to book</a>
            <p><%= element.notes %></p>
        </div>
    <% }); %>
</body>
</html>
